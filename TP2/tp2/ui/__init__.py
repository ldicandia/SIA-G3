"""Optional presentation consumers; never imported by the GA engine.

`tp2/ui/viewer.py` is the only file in the repository allowed to import
pygame -- the mirror image of `tp2/engine/__init__.py`'s "never pygame" rule.
"""
